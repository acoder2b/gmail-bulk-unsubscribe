[project]
name = "gmail-bulk-unsubscribe"
version = "0.2.0"
description = "Clean up your Gmail inbox and bulk-unsubscribe via RFC 8058 one-click List-Unsubscribe. Fork of mxngls/Gmail-Cleaner."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-api-python-client",
    "google-auth-oauthlib",
    "google-auth",
    "tqdm",
    "requests",
    "ruff>=0.9.9",
]
